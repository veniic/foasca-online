{
  "name": "foaica-online",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=packages/shared",
    "test:engine": "npm run test --workspace=packages/engine",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:client": "npm run dev --workspace=packages/client",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:engine": "npm run build --workspace=packages/shared && npm run build --workspace=packages/engine",
    "build:server": "npm run build --workspace=packages/shared && npm run build --workspace=packages/engine && npm run build --workspace=packages/server",
    "build:client": "npm run build --workspace=packages/shared && npm run build --workspace=packages/client",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/engine && npm run build --workspace=packages/server && npm run build --workspace=packages/client"
  }
}
